global.logger = require(process.cwd() + '/system/logger').logger(process.cwd() + '/tests');
global.chai = require('chai');

describe('init', () => {
	//before(()=>{})
	//after(()=>{})
	//beforeEach(()=>{})
	//afterEach(()=>{})
	it('start', () => {
		//somefunc(()=>{assert.equal()})
	})
});